json.extract! tratamiento, :id, :nombre, :descripcion, :duracion, :diagnostico_id, :medico_id, :estado, :frecuencia_administracion, :created_at, :updated_at
json.url tratamiento_url(tratamiento, format: :json)
